from spectra import Spectra
import numpy as np

vibrational_spec = Spectra(x='HF_bomd',y='HF_bomd',z='HF_bomd',trans='fourier') # this creates a spectrum object in the variable "vibrational_spec".  This object contains the spectrum (both frequency and intensity axes) 

